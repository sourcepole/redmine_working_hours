<h2><%= l(:label_working_hours_snapshots) %></h2>

<% labelled_tabular_form_for :snapshot, @snapshot, :url => {:action =>  'update', :id => @snapshot} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_save) %>
<% end %>
