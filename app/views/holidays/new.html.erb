<h2><%= l(:label_holidays_new) %></h2>

<%= labelled_form_for @holiday, :url => holidays_path do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_create) %>
<% end %>

<p><%= link_to l(:button_back), holidays_path %></p>
